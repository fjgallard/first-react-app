const Dex = () => {
  return (
    <form>

      <label htmlFor="type">
        Type 1
        <select id="type1">
          <option />
        </select>
      </label>

      <label htmlFor="type">
        Type 2
        <select id="type2">
          <option />
        </select>
      </label>

      <button>Submit</button>
    </form>
  );
}

export default Dex;